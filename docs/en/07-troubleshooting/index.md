---
title: Troubleshooting
description: The real errors you may hit, and how to fix them.
audience: setup
stability: planned       # authored in phase D9
last_verified: null
---

# Troubleshooting

Concrete problems and fixes — the errors that actually come up during setup, Docker, and deployment, each with
the cause and the resolution (command → expected output).

> **Status:** scaffolded; authored in **phase D9**.

## Topics (planned)

- The monorepo mapping broke after moving/renaming the repo folder (junction/symlink repoint).
- "Block not supported" in the editor (assets not built — `npm run build`).
- WordPress can't connect to the database (dev stack / credentials).
- Permission / file-ownership issues on Linux and in Docker.
- HTTPS / certificate problems on a VM deploy.
