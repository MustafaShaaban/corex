# Spec 057 US1 Consumer Migration Evidence

Captured: 2026-06-20 05:07 EEST

## Scope

US1 migrated first-party front-end references to the canonical properties generated by
`theme/theme.json`. No selector, block attribute, render callback, script dependency, or
registration path changed.

## Conditional asset verification

| Consumer | Asset contract | Verification |
|---|---|---|
| `corex/entity-field` | `block.json` retains `style: file:./style-index.css` | Source inspection and workspace build PASS |
| `corex/corex-form` | `block.json` retains `style: file:./style-index.css` | Source inspection and workspace build PASS |
| CoreX UI blocks | Owning `block.json` files retain local style metadata and entries retain local style imports | Source inspection, Jest, and workspace build PASS |
| Core block style variants | `BlockStyles::HANDLE` remains registered once and supplied to `register_block_style()` as `style_handle` | Source inspection PASS |
| Careers and Portfolio blocks | Existing local style imports and `block.json` style metadata remain unchanged | Source inspection and workspace build PASS |

`git diff` contains no changed `block.json`, block registration PHP, or block entry JavaScript file. The migration
therefore changes token references and formatting only; WordPress retains the existing conditional asset behavior.

## Migration result

- The generated consumer inventory contains zero undefined production custom-property references.
- Existing stable slugs remain defined.
- Eleven legacy properties remain as versioned compatibility aliases through the documented one-minor-release
  window; first-party front-end consumers use canonical roles.
- The existing admin `small` alias remains intentionally recorded for the later scoped admin-adapter batch.
- CSS uses logical properties where direction matters; no physical left/right property was introduced.
